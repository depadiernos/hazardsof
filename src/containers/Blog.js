import React from 'react'
import styled from 'react-emotion'

import getPost from '../utilities/getPosts'

posts = getPost()
console.log(posts)

export default () => {

  return(
    <div>

    </div>
  )
}
